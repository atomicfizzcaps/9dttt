# 🚀 Quick Deployment Reference

## URLs

- **Frontend (Vercel)**: <https://9dttt.vercel.app>
- **Backend (Render)**: <https://ninedttt.onrender.com>


---

## Deploy Commands

### Deploy Everything (Recommended)

```bash
./deploy.sh
```

### Deploy Backend Only

```bash
git push origin main
# Render auto-deploys in ~2 minutes
```

### Deploy Frontend Only

```bash
vercel --prod
# Or just push to GitHub
```

---

## Local Development

```bash
# Start backend (port 3000)
npm start

# Frontend uses http://localhost:3000
# Backend APIs at http://localhost:3000/api
```

---

## Check Status

```bash
# Backend health
curl https://ninedttt.onrender.com/api/health

# Frontend
curl https://9dttt.vercel.app

# API proxy (through Vercel)
curl https://9dttt.vercel.app/api/health
```

---

## Configuration Files

| File | Purpose | Deployed To |
| ------ | --------- | ------------- |
| `vercel.json` | Frontend config + API proxy | Vercel |
| `render.yaml` | Backend config | Render |
| `server.js` | Backend server | Render |
| `Public/` | Static files | Vercel |

---

## Important Environment Variables

### Render (Backend)

```env
FRONTEND_URL=https://9dttt.vercel.app
JWT_SECRET=<auto-generated>
PORT=10000
NODE_ENV=production
```

### Vercel (Frontend)

No env vars needed! API calls are proxied via `vercel.json`

---

## Troubleshooting

### API Calls Failing

1. Check Render backend is running
2. Verify FRONTEND_URL in Render env vars
3. Check vercel.json rewrites

### CORS Errors

1. Update FRONTEND_URL in Render
2. Add your domain to allowed origins in server.js

### WebSocket Not Connecting

1. Verify socket.io proxy in vercel.json
2. Check CORS settings in server.js

---

## File Changes Summary

### New Files

- ✅ `Public/js/unified-auth.js` - Single auth system
- ✅ `Public/js/auth-ui.js` - Modern login UI
- ✅ `Public/js/game-init.js` - Universal initializer
- ✅ `Public/js/api-config.js` - Auto-detect API endpoints
- ✅ `deploy.sh` - One-command deployment
- ✅ `DEPLOYMENT_SPLIT.md` - Full guide

### Updated Files

- ✅ `vercel.json` - Static-only + API proxy
- ✅ `render.yaml` - Backend-only config
- ✅ `server.js` - CORS for split deployment
- ✅ `index.html` - Unified auth scripts
- ✅ All game HTML files - Use unified auth

### Removed Duplication

- ❌ `firebase-init.js` (merged into unified-auth)
- ❌ `browser-auth-init.js` (merged into unified-auth)
- ❌ `universal-auth.js` (replaced by unified-auth)
- ❌ `auth-client.js` (compatibility layer in unified-auth)

---

## Next Steps

1. ✅ Push code to GitHub
2. ✅ Backend deployed to Render (ninedttt.onrender.com)
3. ✅ Frontend deployed to Vercel (9dttt.vercel.app)
4. ⏳ Wait ~2 min for Vercel to rebuild with new config
5. ⬜ Test auth flow end-to-end
6. ⬜ Set custom domain (d9ttt.com) in Vercel (optional)
7. ⬜ Monitor logs for issues

---

Need help? 
- **Backend working**: ✅ <https://ninedttt.onrender.com/api/health>
- **Frontend deploying**: ⏳ <https://9dttt.vercel.app>
- **Full guide**: [DEPLOYMENT_SPLIT.md](DEPLOYMENT_SPLIT.md)
