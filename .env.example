# ============================================
# MINIMAL CONFIGURATION (Works without Firebase!)
# ============================================

# Server Configuration
NODE_ENV=development
PORT=3000

# Authentication (REQUIRED)
# ❌ CRITICAL SECRET - Never expose publicly, never commit to git
JWT_SECRET=your-super-secret-jwt-key-change-this-to-something-random

# Redis (Optional - will use in-memory storage if not provided)
# REDIS_URL=redis://localhost:6379

# That's all you need! The app works with just the above settings.
# Web3 wallet login, email/password, and guest mode work without Firebase.

# ============================================
# OPTIONAL: Firebase Social Login
# ============================================
# Only add these if you want Google/Apple Sign-In
# Otherwise, leave them commented out or delete them.
# See QUICK_START_NO_FIREBASE.md for details.

# Firebase (Optional for Google/Apple Sign-In ONLY)
# ℹ️  IMPORTANT: These Firebase values are SAFE to expose in client-side code
# ✅ Firebase API Keys are PUBLIC by design - they identify your app, not authenticate it
# ✅ Security comes from Firebase Security Rules and authorized domains, not hiding these values
# See: https://firebase.google.com/docs/projects/api-keys
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_API_KEY=your-api-key
# FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
# FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
# FIREBASE_MESSAGING_SENDER_ID=your-sender-id
# FIREBASE_APP_ID=your-app-id
# FIREBASE_MEASUREMENT_ID=your-measurement-id

# ============================================
# ADVANCED (Not Needed for Basic Setup)
# ============================================

# Database
# MONGODB_URI=mongodb://localhost:27017/9dttt

# Session Management
# SESSION_SECRET=your-session-secret-change-this

# Firebase Admin SDK (Only needed for advanced Firebase features)
# FIREBASE_PRIVATE_KEY=your-private-key
# FIREBASE_CLIENT_EMAIL=your-client-email

# Crypto/Web3 (Optional)
# ⚠️  SENSITIVE - Treat as secrets
ALCHEMY_API_KEY=your-alchemy-key
INFURA_KEY=your-infura-key

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100

# CORS
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# API Keys
# ❌ CRITICAL SECRET - Never expose publicly
OPENAI_API_KEY=your-openai-key-for-ai-features
